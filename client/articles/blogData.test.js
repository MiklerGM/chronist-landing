// import { urls, lastUrls, sourceEN, sourceRU } from './blogData';

// test('two plus two is four', () => {
//   expect(urls.length).toBeNull();
// });

// require.context is too obscure to test
// https://github.com/facebook/create-react-app/issues/517
// it must render
test('blogData test in TODO', () => {
  expect(4).toBe(4);
});

console.log('skip test');
